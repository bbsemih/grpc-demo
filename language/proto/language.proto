syntax = "proto3";

package language;

message LanguageRequest {
    string language = 1;
}

message LanguageResponse {
    string info = 1;
}

service LanguageService {
    rpc Language(LanguageRequest) returns(LanguageResponse);//Unary
}